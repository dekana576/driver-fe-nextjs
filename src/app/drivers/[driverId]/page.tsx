import DetailDriverPage from "@/components/pages/driver/detail-driver-page";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Detail Driver | OTW TL",
};

export default function DetailDriver() {
  return <DetailDriverPage />;
}
